1) Create a new repository on Github. To avoid errors, do not initialize the new repository with README, license, or gitignore files. You can add these files after your project has been pushed to GitHub

2) Go to the current working directory of project on cmd prompt

3) git init (only if .git file is not created in folder)

4) git add . (only if .git file is not created in folder)

5) git commit -m "First commit"

6) At the top of your GitHub repository's Quick Setup page, click  to copy the remote repository URL.

7) git remote add origin <remote repository URL> (git remote -v  (verifies new remote URL))

8) git push origin master
